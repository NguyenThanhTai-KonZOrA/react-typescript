import React, { useState } from 'react';
import { Filter } from '../types';

const Home: React.FC = () => {
  return (
    <section>
      <h1>Export Excel Web Application</h1>
    </section>
  );
};

export default Home;
