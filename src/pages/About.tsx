import React from 'react';

const About: React.FC = () => {
  return (
    <div>
      <h2>About</h2>
      <p>Simple Todo app built with React + TypeScript + Context + localStorage.</p>
    </div>
  );
};

export default About;
